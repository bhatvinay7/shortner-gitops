apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-ui
  namespace: shortner
spec:
  type: NodePort
  ports:
    - port: 15672
      targetPort: 15672
      protocol: TCP
  selector:
    app.kubernetes.io/name: rabbitmq
