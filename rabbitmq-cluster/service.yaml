apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
  namespace: shortner
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: NodePort
  ports:
    - port: 15672
      targetPort: 15672
      protocol: TCP
  selector:
    app.kubernetes.io/name: rabbitmq-system
